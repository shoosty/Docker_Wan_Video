# RunPod serverless runtime + utility libs
runpod==1.7.7
supabase==2.10.0
requests==2.32.3

# Hugging Face stack for model download + cache.
# transformers + accelerate bumped to versions that match
# diffusers >=0.33 (Wan2.1) — older pins broke the import.
huggingface-hub[hf_transfer]>=0.27.0
transformers>=4.51.0
accelerate>=1.4.0
safetensors>=0.4.5

# Wan2.1 pipelines (WanPipeline, WanImageToVideoPipeline,
# AutoencoderKLWan) landed in diffusers 0.33. Pin a known-good
# Wan-supporting release; allow patch updates.
diffusers>=0.35.0,<0.40.0
einops>=0.8.0
opencv-python>=4.10.0.84
imageio>=2.36.1
imageio-ffmpeg>=0.5.1
av>=13.1.0

# T5 text encoder (Wan ships with umt5-xxl)
sentencepiece>=0.2.0

# Image decode for i2v
pillow>=11.0.0

# Memory + perf — xformers tied to torch version, let pip resolve
xformers>=0.0.28
